# კლასის კონსტრუქტორი არის
# სპეციალური მეთოდი, რომელიც კლასის ობიექტის
# შექმნისას ავტომატურად იძახება. ეს მეთოდი გამოიყენება 
# ობიექტის დაწყებითი მნიშვნელობების დასადგენად. Python-ში
# კონსტრუქტორი არის __init__() მეთოდი, რომელიც განახორციელებს
# ობიექტის ინიციალიზაციას.

# მაგალითი:

class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

p = Person("John", 25)
print(p.name)  # John
print(p.age)   # 25